<div class="row">
    <div class="col-md-9">
        {{> flash-message}}
        <div class="panel panel-default">
            <div class="panel-heading ">
                <strong> <i class="fa fa-list"></i> 文章列表</strong> {{user.username}}
            </div>
            <div class="panel-body">
                <div class="articles">
                    {{#each articles}}
                        {{> article}}
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</div>